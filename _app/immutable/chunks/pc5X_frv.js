import{a as r,f as p}from"./B3KKMlPP.js";import"./C9Vuyvrt.js";import{am as t,an as c,ao as o}from"./BQfckOSY.js";import{h as s}from"./CjXTrQwK.js";const l={title:"C++ Vectors",date:"2026-6-19",excerpt:"Why we use C++ vectors",description:"A rough outline of why C++ vectors are incredibly useful.",front:!0},{title:k,date:f,excerpt:y,description:w,front:b}=l;var m=p(`<p>Let’s dive into why C++ vectors are so useful. Here’s a short breakdown of why dynamic contiguous containers are foundational concept and
valuable tool in modern programming:</p> <h1>Full Control of Memory</h1> <p>C++‘s vectors are the quintessential version of the simplest typed container, a space of memory split into well defined chunks. At a high level they represent a single pointer to a region of memory, a known capacity and a known size. With these three values programmers can work within a memory region with the implicit understanding of where their objects reside, and which are initialized or not.</p> <p>This simple idea may not be all that impressive, however keeping things simple can often be a highly performant option when programming. This can be the building block for an incredible number of other more complex containers.</p> <p>The benefit of a dynamic container like C++‘s vector is that the memory management can be modified. More than just the simple act of keeping track of a pointer, programmers can also make modifications to the allocator, the portion of the program responsible for managing the pointer used by the vector. This allows programmers to make use of memory in a vector format without necessarily needing to hammer the operating system for new allocations. These modifications can so far as to eliminate allocations entirely, by abstracting the allocator to using a fixed size region of memory (this may not be as dynamic as one would expect, but still incredibly useful because of its performance), the allocator may simply be returning a pointer to a section of memory already inside of the program, no system calls or other complex interactions with the operating system required.</p> <h1>Allocate and Free when you need to~</h1> <p>C++ and C don’t typically have containers which allocate just because a variable has been declared. Most will only allocate at the moment when the container has reached its capacity. Programmers will often attempt to predict heavy usage of memory and reserve larger chunks when necessary. This approach means C and C++ programs often don’t have a “garbage collector”, which can often be an expensive operation, as each object and container is typically expected to maintain its own state. Pointers are freed back to their originating allocator typically immediately after the container has expanded.</p> <pre class="language-cpp"></pre> <h1>Direct initialization into the container</h1> <p>C++ makes a point of allowing the programmer to initialize structures inside of the container directly as opposed to creating an object which will get copied or moved into the container. More than that, the C++ library and compiler designers are fundamentally aware of the performance penalties of unnecessary moves and copies, so you’ll typically find that for a simple container like a <code>vector</code>, any operations creating a new object like insertions or pushes will be highly optimized.</p> <pre class="language-cpp"></pre>`,1);function v(i){var a=m(),e=t(c(a),14);s(e,()=>`<code class="language-cpp">std<span class="token double-colon punctuation">::</span>vector<span class="token operator">&lt;</span><span class="token keyword">int</span><span class="token operator">></span> data<span class="token punctuation">;</span> <span class="token comment">//no allocations just because we know we'll need a container</span></code>`,!0),o(e);var n=t(e,6);s(n,()=>`<code class="language-cpp"><span class="token keyword">struct</span> <span class="token class-name">person</span> <span class="token punctuation">&#123;</span>
    std<span class="token double-colon punctuation">::</span>string_view name<span class="token punctuation">;</span>
    <span class="token keyword">int</span>              height_mm<span class="token punctuation">;</span>
    <span class="token keyword">int</span>              exercise_watts_avg<span class="token punctuation">;</span>
    <span class="token keyword">unsigned</span> <span class="token keyword">char</span>    age<span class="token punctuation">;</span>
<span class="token punctuation">&#125;</span><span class="token punctuation">;</span>
std<span class="token double-colon punctuation">::</span>vector<span class="token operator">&lt;</span>person<span class="token operator">></span> health_data<span class="token punctuation">;</span>
health_data<span class="token punctuation">.</span><span class="token function">emplace_back</span><span class="token punctuation">(</span><span class="token string">"John"</span><span class="token punctuation">,</span> <span class="token number">1677</span><span class="token punctuation">,</span> <span class="token number">145</span><span class="token punctuation">,</span> <span class="token number">38</span><span class="token punctuation">)</span><span class="token punctuation">;</span> <span class="token comment">//</span></code>`,!0),o(n),r(i,a)}export{v as default,l as metadata};

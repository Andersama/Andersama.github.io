import{a as k,f as h}from"./CppqSQWl.js";import"./DwPF1q1B.js";import{am as a,an as m,ao as n,n as f}from"./DPow9XtE.js";import{h as s}from"./N9FhVCPv.js";const _={title:"unchecked_emplace_back, unchecked_emplace and self_assign",date:"2021-08-20T00:00:00.000Z",excerpt:"A quick std-proposal"},{title:I,date:x,excerpt:z}=_;var g=h('<h2>Abstract</h2> <h3>1. Revision History</h3> <ol start="0"><li>Initial Draft</li></ol> <p><code>unchecked_emplace_back</code> and <code>unchecked_emplace</code> are variations of two functions which already exist in the standard for several containers. The key difference being these <code>unchecked_</code> variants do not / will not allocate, they assume the capacity of already allocated storage is at least 1 more than the current size. For the purposes of this paper we can consider the signatures to <code>unchecked_emplace_back</code> and <code>unchecked_emplace</code> match those of their “checked” counterparts. The most relevant container is <code>vector</code>, for the purpose of this proposal consider the proposal to add these functions to all containers which function similarly to <code>vector</code>. Assume the worst of these functions in terms of safety, they can and will corrupt data, reach and dereference memory out of bounds and are the essence of running with scissors.</p> <pre class="language-c"></pre> <p><code>self_assign</code> is a new function, which is similar to <code>assign</code>, like the above <code>unchecked_</code> functions, <code>self_assign</code> will not allocate. It still “replaces” the containers contents, however <code>self_assign</code> is much more restricted in usage.</p> <pre class="language-c"></pre> <pre class="language-c"></pre> <p>This example hopefully gives away the rough purpose and usage of <code>self_assign</code>, it is a single parameter function which takes an iterator to the new end of the array. It assumes the memory is already managed, no lifetime management of the data is performed, if <code>self_assign</code> were used to expand the container it will be the responsiblity of the maintainer to construct data otherwise if <code>self_assign</code> were used to shrink the container the mainainter is expected to have destroyed data as needed. It also expects the iterator to be within the existing bounds between <code>begin()</code> and <code>begin()+capacity()</code>, this may be (and should be) asserted. It’s also a good idea to limit the function to only be enabled if the input type is in fact an iterator.</p> <h3>2. Motivation</h3> <p><code>unchecked_emplace_back</code> and <code>unchecked_emplace</code> to a certain degree already exist in most standard library implmentations, with these the goal is more or less to present more of the internal utilies already written with performance in mind to the user and to prevent unnecessary work arounds to get to the same performance as the backend has available. Consider below:</p> <pre class="language-c"></pre> <p>While <code>std::vector&lt;char&gt;</code> is not <code>std::string</code> and this small snippet is not slow by any stretch of the imagination, parts of this snippet may be relying on some compiler optimizations to kick in. If we’re lucky the compiler might notice what we do intuitively in this example which is that we don’t need <code>emplace_back</code> to check on the capacity of the vector, even better and more likely is that the compiler will remove the allocation entirely given how many constants are given like this. However the key concept here is that we know, or rather have written a loop to fill our container with data we need, and we don’t need to check our container’s <code>capacity()</code>.</p> <pre class="language-c"></pre> <p>We can also roughly imagine similar benefits for calls to <code>emplace</code>, which is the backbone for <code>insert</code>.</p> <h3>2.1 Current state of affairs</h3> <p>Here is where things become painful, <code>std::vector</code> is pretty well guarded in that while we may absolutely abuse the access to <code>data()</code> to manipulate any number of indexs, there is no nice way of controlling <code>size()</code>. This makes for an awkwardly lobsided api for anyone wanting or needing to etch out the last bit of performance and try for lower level access. There are no public methods with which we can modify the internals of the container without avoiding unncessary bounds checking, unncessary copies, unncessary construction or destruction calls. If we need to change the <code>size()</code> in order to maintain the validity of the vector after our manipulations, we must pay a higher level cost which may defeat the purpose.</p> <p>Obviously this is no accident, a well designed api for a class after all should be constrained so that misuse and errors can be handled well. However if we were aiming for safety through and through, we likely shouldn’t have <code>operator[]</code> nor <code>data()</code> while we have <code>at()</code>. This paper ideally lends out a hand to all those who work at the lower level so that we don’t have to write our own <code>std::vector</code> or similar just to scrape a few % points that are clearly available, or to those who’re still very much in love with c and are not afraid of pointer dereferences or array access.</p> <h3>2.2 The assign approach</h3> <p>Lets consider what it currently looks like if we wanted to do something similar to <code>unchecked_emplace_back</code>.</p> <pre class="language-c"></pre> <p>Depending on the datatypes we’re using <code>assign</code> can be extremely penalizing as it can do a full gambit of additional work. Here at least with a trivial data it’s not so bad, but anything complex we will end up paying for. <code>assign</code> is designed towards assigning data as quick as humanly possible, an implmentation might make self assignment a specialized case to do as little work as possible, however much like the <code>unchecked_</code> variations above we can avoid a branch because we know a bit more about the problem.</p> <p>When I originally was motivated to modify <code>&lt;vector&gt;</code> bizzarely it was because I wrote something like the above, but didn’t consider anything about <code>assign</code>. I immediately was drawn to the idea of <code>unchecked_emplace_back</code> because I knew it already existed and wouldn’t take much work, and my code was absolutely strewn with <code>emplace_back</code>. However as shown above, <code>self_assign</code> also did not take much work. As a bonus it also has a nice looking pattern:</p> <pre class="language-c"></pre> <p>Here, <code>reserve()</code> and <code>self_assign()</code> form their own neatly block. It can be even better, because in a real example even if we’re appending conditionally we can still take advantage of a branchless loop:</p> <pre class="language-c"></pre> <p>Here we can get something out of <code>self_assign</code> we currently can’t out of <code>emplace_back</code> or similar, of course the benefit is solely because we’ve already saved ourselves a branch and avoided any allocation. If we have to pay for a branch because we have an unknown <code>capacity()</code> requirement, we should be using <code>emplace_back</code>.</p> <h3>3. Considerations</h3> <p>All of these functions likely violate some degree of safety which the api has been striving to achieve, namely that they don’t just “work”. There are specific conditions to their use and they operate in tandem with a well placed <code>reserve()</code>. As someone explained (and I’ll need help to cite who because I can’t remember) there’s an additional difficulty in managing pairs of things. However c and c++ are littered with examples, <code>malloc</code> and <code>free</code>, <code>new</code> and <code>delete</code> and I’m sure a number of others which I also can’t remember from the talk. This makes them error prone and are the source of many existing security concerns. In very weird applications the managment of these pairs is almost given up entirely, namely in compilers like DMD where the program is simply not meant to be executing for long.</p> <p>That said these functions are far much more like <code>malloc</code> and <code>free</code>, <code>new</code> and <code>delete</code> in that while they may be “new” to the publicly facing api, <code>unchecked_emplace_back</code> and <code>unchecked_emplace</code> are very much not new, especially to library implementers, nor to anyone who has peaked at a <code>&lt;vector&gt;</code> header. These utility functions have be lying in the background optimzing edge cases in much the way we might expect.</p> <p>Of the functions presented <code>self_assign</code> is very much an explicit deviation from the safety oriented api, namely it does obfuscate its ability to modify <code>size()</code>, although I very much think <code>modify_size()</code> would be far more egregrious. I would fully expect that <code>self_assign</code> come with assertions for anyone safety concious. Unlike the <code>unchecked_</code> variations it does in fact provide a new form of functionality, namely that it allows for more complicated (and performant) appends. I don’t imagine it would be long for someone to use it for more complicated transformations as well.</p>',1);function T(d){var r=g(),e=a(m(r),8);s(e,()=>`<code class="language-c"><span class="token comment">//...imagine somewhere buried in MSVC's vector standard header</span>
template <span class="token operator">&lt;</span>class<span class="token punctuation">.</span><span class="token punctuation">.</span><span class="token punctuation">.</span> _Valty<span class="token operator">></span>
_CONSTEXPR20_CONTAINER <span class="token function">decltype</span><span class="token punctuation">(</span><span class="token keyword">auto</span><span class="token punctuation">)</span> <span class="token function">unchecked_emplace_back</span><span class="token punctuation">(</span>_Valty <span class="token operator">&amp;&amp;</span><span class="token punctuation">.</span><span class="token punctuation">.</span><span class="token punctuation">.</span>_Val<span class="token punctuation">)</span> <span class="token punctuation">&#123;</span>
  <span class="token keyword">return</span> <span class="token function">_Emplace_back_with_unused_capacity</span><span class="token punctuation">(</span>_STD forward<span class="token operator">&lt;</span>_Valty<span class="token operator">></span><span class="token punctuation">(</span>_Val<span class="token punctuation">)</span><span class="token punctuation">.</span><span class="token punctuation">.</span><span class="token punctuation">.</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
<span class="token punctuation">&#125;</span></code>`,!0),n(e);var t=a(e,4);s(t,()=>`<code class="language-c"><span class="token comment">//...imagine somewhere buried in MSVC's vector standard header</span>
template <span class="token operator">&lt;</span>class _Iter<span class="token operator">></span>
_CONSTEXPR20_CONTAINER <span class="token keyword">void</span> <span class="token function">_Self_Assign</span><span class="token punctuation">(</span>_Iter _Last<span class="token punctuation">)</span> <span class="token punctuation">&#123;</span>
  <span class="token keyword">auto</span> <span class="token operator">&amp;</span>   _My_data <span class="token operator">=</span> _Mypair<span class="token punctuation">.</span>_Myval2<span class="token punctuation">;</span>
  pointer <span class="token operator">&amp;</span>_Mylast  <span class="token operator">=</span> _My_data<span class="token punctuation">.</span>_Mylast<span class="token punctuation">;</span>
  pointer <span class="token operator">&amp;</span>_Myend   <span class="token operator">=</span> _My_data<span class="token punctuation">.</span>_Myend<span class="token punctuation">;</span>
  
  <span class="token function">_Adl_verify_range</span><span class="token punctuation">(</span>_Last<span class="token punctuation">,</span> _My_data<span class="token punctuation">.</span>_Myend<span class="token punctuation">)</span><span class="token punctuation">;</span>
  _Mylast <span class="token operator">=</span> _Last<span class="token punctuation">;</span>
<span class="token punctuation">&#125;</span>

template <span class="token operator">&lt;</span>class _Iter<span class="token punctuation">,</span> <span class="token class-name">enable_if_t</span><span class="token operator">&lt;</span>_Is_iterator_v<span class="token operator">&lt;</span>_Iter<span class="token operator">></span><span class="token punctuation">,</span> <span class="token keyword">int</span><span class="token operator">></span> <span class="token operator">=</span> <span class="token number">0</span><span class="token operator">></span>
_CONSTEXPR20_CONTAINER <span class="token keyword">void</span> <span class="token function">self_assign</span><span class="token punctuation">(</span>_Iter _Last<span class="token punctuation">)</span> <span class="token punctuation">&#123;</span>
  <span class="token function">_Self_Assign</span><span class="token punctuation">(</span>_Last<span class="token punctuation">)</span><span class="token punctuation">;</span>
<span class="token punctuation">&#125;</span></code>`,!0),n(t);var o=a(t,2);s(o,()=>`<code class="language-c"><span class="token comment">//...perhaps written a bit more human like</span>
<span class="token comment">//...assuming our container's implemention of vector stores three iterators _begin, _end, and _capacity</span>
<span class="token comment">//where size() returns _end - _begin, capacity() returns _capacity and data() and begin() return _begin</span>
template <span class="token operator">&lt;</span>class Iterator<span class="token punctuation">,</span> <span class="token class-name">enable_if_t</span><span class="token operator">&lt;</span>_Is_iterator_v<span class="token operator">&lt;</span>Iterator<span class="token operator">></span><span class="token punctuation">,</span> <span class="token keyword">int</span><span class="token operator">></span> <span class="token operator">=</span> <span class="token number">0</span><span class="token operator">></span>
constexpr <span class="token keyword">void</span> <span class="token function">self_assign</span><span class="token punctuation">(</span>Iterator last<span class="token punctuation">)</span> <span class="token punctuation">&#123;</span>
    <span class="token function">assert</span><span class="token punctuation">(</span>last <span class="token operator">>=</span> <span class="token function">begin</span><span class="token punctuation">(</span><span class="token punctuation">)</span> <span class="token operator">&amp;&amp;</span> last <span class="token operator">&lt;=</span> <span class="token punctuation">(</span><span class="token function">begin</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token operator">+</span><span class="token function">capcity</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">)</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
    _end <span class="token operator">=</span> last<span class="token punctuation">;</span>
<span class="token punctuation">&#125;</span></code>`,!0),n(o);var p=a(o,8);s(p,()=>`<code class="language-c">std<span class="token operator">::</span>vector<span class="token operator">&lt;</span><span class="token keyword">char</span><span class="token operator">></span> a_pseudo_string<span class="token punctuation">;</span>
a_pseudo_string<span class="token punctuation">.</span><span class="token function">reserve</span><span class="token punctuation">(</span><span class="token number">32</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
<span class="token keyword">for</span> <span class="token punctuation">(</span><span class="token class-name">size_t</span> i <span class="token operator">=</span> <span class="token number">0</span><span class="token punctuation">;</span> i <span class="token operator">&lt;</span> <span class="token number">32</span><span class="token punctuation">;</span> i<span class="token operator">++</span><span class="token punctuation">)</span> <span class="token punctuation">&#123;</span>
  a_pseudo_string<span class="token punctuation">.</span><span class="token function">emplace_back</span><span class="token punctuation">(</span>i<span class="token operator">+</span><span class="token number">32</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
<span class="token punctuation">&#125;</span></code>`,!0),n(p);var c=a(p,4);s(c,()=>`<code class="language-c">std<span class="token operator">::</span>vector<span class="token operator">&lt;</span><span class="token keyword">char</span><span class="token operator">></span> a_pseudo_string<span class="token punctuation">;</span>
a_pseudo_string<span class="token punctuation">.</span><span class="token function">reserve</span><span class="token punctuation">(</span><span class="token number">32</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
<span class="token keyword">for</span> <span class="token punctuation">(</span><span class="token class-name">size_t</span> i <span class="token operator">=</span> <span class="token number">0</span><span class="token punctuation">;</span> i <span class="token operator">&lt;</span> <span class="token number">32</span><span class="token punctuation">;</span> i<span class="token operator">++</span><span class="token punctuation">)</span> <span class="token punctuation">&#123;</span>
  <span class="token comment">//does what it says on the tin</span>
  a_pseudo_string<span class="token punctuation">.</span><span class="token function">unchecked_emplace_back</span><span class="token punctuation">(</span>i<span class="token operator">+</span><span class="token number">32</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
<span class="token punctuation">&#125;</span></code>`,!0),n(c);var i=a(c,14);s(i,()=>`<code class="language-c">std<span class="token operator">::</span>vector<span class="token operator">&lt;</span><span class="token keyword">char</span><span class="token operator">></span> a_pseudo_string<span class="token punctuation">;</span>
a_pseudo_string<span class="token punctuation">.</span><span class="token function">reserve</span><span class="token punctuation">(</span><span class="token number">32</span><span class="token punctuation">)</span><span class="token punctuation">;</span>

<span class="token keyword">char</span> <span class="token operator">*</span>danger_ptr <span class="token operator">=</span> a_pseudo_string<span class="token punctuation">.</span><span class="token function">data</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
<span class="token keyword">for</span> <span class="token punctuation">(</span><span class="token class-name">size_t</span> i <span class="token operator">=</span> <span class="token number">0</span><span class="token punctuation">;</span> i <span class="token operator">&lt;</span> <span class="token number">32</span><span class="token punctuation">;</span> i<span class="token operator">++</span><span class="token punctuation">)</span> <span class="token punctuation">&#123;</span>
  danger_ptr<span class="token punctuation">[</span>i<span class="token punctuation">]</span> <span class="token operator">=</span> i<span class="token operator">+</span><span class="token number">32</span><span class="token punctuation">;</span>
<span class="token punctuation">&#125;</span>
<span class="token comment">// an incredibly awkward use of assign</span>
a_psudo_string<span class="token punctuation">.</span><span class="token function">assign</span><span class="token punctuation">(</span>danger_ptr<span class="token punctuation">,</span> danger_ptr<span class="token operator">+</span><span class="token number">32</span><span class="token punctuation">)</span><span class="token punctuation">;</span></code>`,!0),n(i);var l=a(i,6);s(l,()=>`<code class="language-c">std<span class="token operator">::</span>vector<span class="token operator">&lt;</span>some_type<span class="token operator">></span> buffer<span class="token punctuation">;</span>
buffer<span class="token punctuation">.</span><span class="token function">reserve</span><span class="token punctuation">(</span>maximum_width<span class="token punctuation">)</span><span class="token punctuation">;</span>
<span class="token class-name">size_t</span> count <span class="token operator">=</span> <span class="token number">0</span><span class="token punctuation">;</span>
<span class="token keyword">for</span> <span class="token punctuation">(</span><span class="token class-name">size_t</span> i <span class="token operator">=</span> <span class="token number">0</span><span class="token punctuation">;</span> i <span class="token operator">&lt;</span> maximum_width<span class="token punctuation">;</span> i<span class="token operator">++</span><span class="token punctuation">)</span> <span class="token punctuation">&#123;</span>
  <span class="token comment">//etc...</span>
<span class="token punctuation">&#125;</span>
buffer<span class="token punctuation">.</span><span class="token function">self_assign</span><span class="token punctuation">(</span>buffer<span class="token punctuation">.</span><span class="token function">data</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token operator">+</span>count<span class="token punctuation">)</span><span class="token punctuation">;</span></code>`,!0),n(l);var u=a(l,4);s(u,()=>`<code class="language-c">std<span class="token operator">::</span>vector<span class="token operator">&lt;</span><span class="token class-name">size_t</span><span class="token operator">></span> buffer<span class="token punctuation">;</span>
buffer<span class="token punctuation">.</span><span class="token function">reserve</span><span class="token punctuation">(</span>maximum_width<span class="token punctuation">)</span><span class="token punctuation">;</span>
<span class="token class-name">size_t</span> count <span class="token operator">=</span> <span class="token number">0</span><span class="token punctuation">;</span>
<span class="token comment">//a loop to store odd numbers</span>
<span class="token keyword">for</span> <span class="token punctuation">(</span><span class="token class-name">size_t</span> i <span class="token operator">=</span> <span class="token number">0</span><span class="token punctuation">;</span> i <span class="token operator">&lt;</span> maximum_width<span class="token punctuation">;</span> i<span class="token operator">++</span><span class="token punctuation">)</span> <span class="token punctuation">&#123;</span>
  <span class="token comment">//no ifs required, no branches</span>
  bool is_odd <span class="token operator">=</span> i <span class="token operator">%</span> <span class="token number">2</span><span class="token punctuation">;</span>
  buffer<span class="token punctuation">.</span><span class="token function">data</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">[</span>count<span class="token punctuation">]</span> <span class="token operator">=</span> i<span class="token punctuation">;</span>
  count <span class="token operator">+=</span> is_odd<span class="token punctuation">;</span>
<span class="token punctuation">&#125;</span>
buffer<span class="token punctuation">.</span><span class="token function">self_assign</span><span class="token punctuation">(</span>buffer<span class="token punctuation">.</span><span class="token function">data</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token operator">+</span>count<span class="token punctuation">)</span><span class="token punctuation">;</span></code>`,!0),n(u),f(10),k(d,r)}export{T as default,_ as metadata};
